// import all the custom components here
import { default as header } from "./header/header";
import { default as markdownSection } from "./markdownSection/markdownSection";

const schemas = {
  header,
  markdownSection,
};

export default schemas;
